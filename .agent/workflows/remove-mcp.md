---
description: Remove an MCP server from Antigravity configuration
value: ⭐⭐⭐
---
1. If the user hasn't specified which MCP server to remove, ask them for the specific `mcp_server_name` exactly as it appears in the configuration.
// turbo
2. Execute the removal script:
   `python3 ~/.agent/scripts/remove_mcp.py <mcp_server_name>`
3. Report the result to the user.
